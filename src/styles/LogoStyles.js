import { StyleSheet } from "react-native";
import theme from "./Theme";

module.exports = StyleSheet.create({
  logo: {
    width: theme.rendersize.l,
    height: theme.rendersize.s,
  },
  driverlogo: {
    width: theme.rendersize.s,
    height: theme.rendersize.s,
  },
});
